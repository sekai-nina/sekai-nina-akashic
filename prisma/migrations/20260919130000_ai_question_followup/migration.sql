-- 会話の続きとして聞かれた質問に印を付ける
--
-- 一覧は1問1答ずつ独立した行なので、「わたしも深大寺行ったことあるよ！」のような
-- 続きの質問が、単独の質問として並んでしまっていた（時刻の降順なので、続きの方が
-- 親より上に出る）。読み違える。
--
-- ただし行同士を繋ぐには会話IDが要り、それは方針文(/ai)で「保存しない」と明言している
-- ものそのもの。なので「続きかどうか」の印だけを持ち、どの質問の続きなのかは持たない。
ALTER TABLE "AiQuestion" ADD COLUMN "followUp" BOOLEAN NOT NULL DEFAULT false;
